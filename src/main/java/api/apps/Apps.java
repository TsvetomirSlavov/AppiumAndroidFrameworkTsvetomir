package api.apps;

import api.apps.famy.Famy;
import api.apps.speedtest.Speedtest;

/**
 * List Represents all apps api's that we have created
 * Here we can add more apps like Famy and implemet their APIs in a separate packages with the same POM implementation
 */
public class Apps {

    public Speedtest speedtest = new Speedtest();
    public Famy famy = new Famy();













}
